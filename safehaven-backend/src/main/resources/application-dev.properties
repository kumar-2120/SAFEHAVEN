# ── Development profile overrides ─────────────────────────────────────────
# Activate with: spring.profiles.active=dev  (or -Dspring.profiles.active=dev)

# H2 console is safe to expose locally during development
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

spring.jpa.show-sql=true
